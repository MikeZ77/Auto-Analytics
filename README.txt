TO RUN:

# Python > 3.6

ARGUMENTS:

# threads = the number of threads 
# proxies = the number of proxies to rotate per price interval
# timeout = how long to wait for a connection before canceling the request

python3.6 autotrader.py --threads --proxies --timeout



